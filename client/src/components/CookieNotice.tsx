import { useEffect, useState } from "react";
export default function CookieNotice() { const [show, setShow] = useState(false); useEffect(() => { setShow(localStorage.getItem("revsimplex-cookie-choice") !== "accepted"); }, []); if (!show) return null; return <aside className="cookie-notice"><div><strong>We value your privacy.</strong><p>We use essential cookies to keep Revsimplex secure, responsive, and working smoothly.</p></div><div className="cookie-actions"><button onClick={() => { localStorage.setItem("revsimplex-cookie-choice", "accepted"); setShow(false); }}>Accept</button><button className="cookie-decline" onClick={() => { localStorage.setItem("revsimplex-cookie-choice", "declined"); setShow(false); }}>Decline</button></div></aside>; }
