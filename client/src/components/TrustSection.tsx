import { ChevronLeft, ChevronRight, Star } from "lucide-react";
import { useState } from "react";
import { testimonials } from "@/data/investment";
export default function TrustSection() { const [active, setActive] = useState(0); const item = testimonials[active]; return <section className="trust-section section-pad"><div className="container"><div className="section-kicker"><span>04 / A calmer way to invest</span><span className="line" /></div><div className="trust-layout"><div><p className="eyebrow"><span className="eyebrow-dot" /> Voices from the journey</p><h2>Tools that<br /><em>stay with you.</em></h2><div className="trust-controls"><button aria-label="Previous testimonial" onClick={() => setActive((active - 1 + testimonials.length) % testimonials.length)}><ChevronLeft size={18} /></button><button aria-label="Next testimonial" onClick={() => setActive((active + 1) % testimonials.length)}><ChevronRight size={18} /></button><span>0{active + 1} / 0{testimonials.length}</span></div></div><article className="testimonial-card"><div className="testimonial-avatar">{item.initials}</div><div className="stars">{[1,2,3,4,5].map(n => <Star key={n} size={14} fill="currentColor" />)}</div><blockquote>“{item.quote}”</blockquote><strong>{item.name}</strong><small>{item.role}</small><em>Revsimplex member story</em></article></div><div className="logo-marquee"><div className="logo-track"><span>northstar</span><span>OPAL / RESEARCH</span><span>FIELD NOTES</span><span>atlas</span><span>COMMON GROUND</span><span>northstar</span><span>OPAL / RESEARCH</span><span>FIELD NOTES</span></div></div></div></section>; }
